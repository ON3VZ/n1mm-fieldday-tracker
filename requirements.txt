# N1MM Field Day Tracker — runtime dependencies (§3.4)
# Standard library covers: UDP, XML parsing, JSON, CSV, HTTP server.

openpyxl>=3.1        # Excel import of the participant list (§7)
fpdf2>=2.7           # PDF export (§10.4)
keyring>=24          # GitHub token storage in the OS keyring (§10.3)
requests>=2.31       # GitHub Contents API (§10.3)

# --- Development / test dependencies (optional) ---
# Install with: pip install -r requirements.txt pytest
pytest>=8    # tests draaien met: python -m pytest tests/
